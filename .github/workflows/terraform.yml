- name: Destroy DEV
  run: |
    cd environments/dev
    terraform init
    terraform destroy -auto-approve

- name: Destroy QA
  run: |
    cd environments/qa
    terraform init
    terraform destroy -auto-approve

- name: Destroy PROD
  run: |
    cd environments/prod
    terraform init
    terraform destroy -auto-approve
